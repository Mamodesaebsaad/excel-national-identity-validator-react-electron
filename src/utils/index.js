import * as XLSX from "xlsx";

// eslint-disable-next-line no-undef
export default function exportExcel  (file) {
  console.log(file)
//   const fileReader = new FileReader();

//   fileReader.readAsArrayBuffer(file);

//   fileReader.onload = (e) => {
//     console.log(e.target.result);
//   }
};
